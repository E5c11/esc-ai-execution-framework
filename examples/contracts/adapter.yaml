schema_version: 1
adapter:
  id: opencode-local
  kind: agent-runtime
  provider: opencode
  capabilities: [sessions, events, tools, permissions]
  configuration:
    transport: http
    model:
      provider_id: opencode
      model_id: big-pickle
